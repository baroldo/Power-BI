CustomerPercentileRank = 
ROUND(
    DIVIDE(
        RANKX(
            ALLSELECTED(MARGIN_DECILIES),
            CALCULATE(SUM(MARGIN_DECILIES[MARGIN_$])),
            ,
            DESC,
            DENSE
        ),
        COUNTROWS(ALLSELECTED(MARGIN_DECILIES))
    ) * 100,
    0
)
