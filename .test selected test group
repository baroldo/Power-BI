CurrentCategoryDebug = 
VAR SelectedCategory = SELECTEDVALUE('GroupBySelector'[GroupBySelector Fields], "All")

RETURN
SWITCH(
    SelectedCategory,
    "'MARGIN_DECILIES'[SECTOR_DESC]", CONCATENATEX(VALUES('MARGIN_DECILIES'[SECTOR_DESC]), 'MARGIN_DECILIES'[SECTOR_DESC], ", "),
    "'MARGIN_DECILIES'[PROD_CAT]", CONCATENATEX(VALUES('MARGIN_DECILIES'[PROD_CAT]), 'MARGIN_DECILIES'[PROD_CAT], ", "),
    "'MARGIN_DECILIES'[INDUSTRY]", CONCATENATEX(VALUES('MARGIN_DECILIES'[INDUSTRY]), 'MARGIN_DECILIES'[INDUSTRY], ", "),
    "All", "All"
)

